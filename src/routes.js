import StatusContent from './views/Order/Status';

const viewRoutes = [
    {
        path: '/',
        name: 'Home',
        icon: '',
        component: StatusContent,
        layout: '/home',
    },
];

export default viewRoutes;
